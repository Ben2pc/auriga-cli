# Some Other Heading

This file exists but its first heading is not the canonical
`# auriga Workflow (vX.Y.Z)` line. The scanner cannot extract a
version, so behavior is part of the contract under test.
