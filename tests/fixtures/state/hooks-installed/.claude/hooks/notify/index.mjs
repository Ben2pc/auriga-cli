// notify hook stub used by tests/state.test.ts. Its SHA256 is captured
// dynamically in the test (we compute it after writing the file) so this
// content is free to change without breaking the test contract.
export default function noop() {}
