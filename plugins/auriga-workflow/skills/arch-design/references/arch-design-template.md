# `arch_design.md` 模板

存在实质性的架构、领域模型或跨边界决策时，将下面的模板复制到 `docs/specs/<topic>/arch_design.md`，并在实现开始前交给用户评审。

使用约定：

- 保留双语章节标题，正文使用对话语言。
- 删除写作提示、占位符和不适用的可选章节。
- 先写评审重点，再写支撑细节；图只在能明显提高理解速度时使用。
- 用户确认后更新评审状态；未确认前不进入实现。
- `Alternatives & Tradeoffs` 仅在存在真实取舍时保留本节，不为完整形式制造候选。

````markdown
# 架构设计：<主题>

## Review Focus / 人工评审重点

> 用最短篇幅告诉评审者：本次改变什么、最需要确认什么、最大的风险是什么。

- **评审状态**：待人工评审 | 已确认
- **核心决定**：<需要确认的结构或领域决定>
- **主要影响**：<受影响的模块、调用方或数据>
- **首要风险**：<最值得质疑的假设或代价>

## Context & Problem / 背景与问题

> 写当前结构和具体问题。新功能引用需求目标；现有系统点名结构或模型缺陷，并锚定代码路径、接口或数据流。

<背景、当前结构、问题及证据>

## Goals & Non-Goals / 目标与非目标

**目标**
- <本设计必须解决什么>

**非目标**
- <本设计明确不解决什么>

## Domain Model / 领域模型

> 仅在涉及领域概念、职责、不变量或生命周期时保留。不要把所有名词机械映射为类型。

| 概念 | 职责与行为 | 身份或生命周期 | 必须保持的不变量 |
|---|---|---|---|
| <概念> | <它负责什么> | <如何识别、何时变化> | <始终成立的规则> |

## Constraints & Invariants / 约束与不变量

- **用户可观察行为**：<不能被技术设计悄悄改变的行为>
- **公共契约**：<接口、数据格式或兼容性要求>
- **项目规则**：<适用的 `docs/rules/arch/` 约束；没有则写“无项目专属架构规则”>
- **其他约束**：<平台、性能预算、安全或运维限制>

## Proposed Design / 建议设计

> 说明领域职责、模块边界、依赖方向、关键接口和数据流。目录树或图只画与本次决定有关的部分。

### Responsibilities & Boundaries / 职责与边界

<每个模块或领域对象负责什么，明确不负责什么>

### Dependencies & Interfaces / 依赖与接口

<依赖指向哪里，接缝暴露什么契约，隐藏什么实现>

### Data Flow / 数据流

<关键输入如何经过系统并产生输出或副作用>

## Alternatives & Tradeoffs / 备选与取舍

> 仅在存在真实取舍时保留本节。每个候选都应能成立，并写具体后果；没有真实取舍就删除整节。

| 方案 | 具体收益 | 具体代价 | 结论 |
|---|---|---|---|
| <方案> | <收益> | <代价> | <采用或否决理由> |

## Migration & Behavior Protection / 迁移与行为保护

> 仅在修改现有系统时保留。描述从现状到目标态的约束和保护网，不拆实施步骤。

- **迁移方式**：<显式切换、并行变更、抽象分支、绞杀榕或其他方式及理由>
- **中间状态与兼容窗口**：<迁移期间允许哪些双路径或临时结构，持续到何时>
- **切换信号**：<满足什么证据后切到目标路径>
- **行为保护**：<哪些测试、监控或检查确保外部行为不变>
- **回滚条件**：<出现什么信号时停止或回退>
- **旧路径删除条件**：<满足什么条件后删除兼容层、开关或旧实现>
- **负责人**：<谁负责跟踪迁移完成并清理过渡结构>

## Risks & Validation / 风险与验证

| 风险或假设 | 影响 | 缓解或验证方式 |
|---|---|---|
| <风险> | <具体影响> | <如何在实现前或实现中确认> |

## Human Decisions / 人工确认结果

> 记录评审者最终确认或修改的决定。设计变更后同步更新正文，不只在这里追加备注。

- <确认的决定、修改意见及仍需遵守的条件>
````
