# 验收契约 — 原生 AGENTS.md 单一入口

## VAL-OWNERSHIP-001 — 项目工作流只管理 AGENTS.md

- **要求**：安装、升级和卸载只操作项目根目录 `AGENTS.md`。
- **通过标准**：项目存在真实文件或软链 `CLAUDE.md` 时，其内容或目标保持不变，且安装器不输出相关兼容提示；卸载日志不提及它。
- **验证方式**：文件系统集成测试。

## VAL-SCAN-001 — 项目状态只读取 AGENTS.md

- **要求**：只有项目根目录 `AGENTS.md` 可表示项目工作流已安装。
- **通过标准**：只有 `CLAUDE.md` 时状态为未安装；受管 `AGENTS.md` 时状态为已安装。
- **验证方式**：状态扫描测试。

## VAL-DOCUMENTATION-001 — 活跃规范没有低版本兼容路线

- **要求**：活跃模板、技能、帮助、README、Web UI 和开发指南只描述 `AGENTS.md`。
- **通过标准**：不再出现 Claude Code 最低版本、功能开关、`@AGENTS.md` 导入或项目 `CLAUDE.md` 兼容软链建议。
- **验证方式**：仓库契约测试、技能验证器与反向文本搜索。

## VAL-REGRESSION-001 — AGENTS.md 管理行为保持完整

- **要求**：受管区块升级、用户区保留、外国内容并入、损坏标记备份和旧格式 `AGENTS.md` 迁移保持有效。
- **通过标准**：相关根测试和端到端安装测试全部通过。
- **验证方式**：单元、集成与 tarball 端到端测试。
