# systematic-debugging 评审

## 当前职责

在实施永久修复前约束根因判断必须有证据，并为可复现问题、低频线上问题和生产事故提供一致的诊断边界。

## 可复现失效模式

- 看到错误后直接修改最可疑的代码，没有验证路径证明修改覆盖了用户症状。
- 线上问题暂时无法复现时，把推测写成已确认根因。
- 生产事故只顾调查而延误恢复，或把回滚、关闭开关等临时缓解描述为永久修复。
- 添加大量与问题无关的日志或浅层测试，既不能缩小根因，也不能捕获真实回归。

## 目标模型证据

本轮没有把社区观点当作目标模型的受控评测结论。用户提供的新模型使用反馈和对现有技能的逐条审查表明：目标模型已经能自行选择常见诊断工具，固定四阶段、强制最小复现、固定假设数量和级联技能调用会重复模型能力；但“根因结论必须对应验证或证据采集路径”仍是跨模型、跨运行时的稳定工程契约。

后续若建立模型评测基线，应继续验证这一结论，不把本次人工评审冒充为长期模型能力证明。

## 与其他资产的关系

- `test-driven-development` 负责实现阶段的红—绿—重构，不替代问题现场与根因证据。
- `verification-before-completion` 负责完成声明前的证据，不替代修复前的诊断判断。
- `incremental-impl` 只在实现切片发生回归时触发本技能，不形成强制级联。

## 处置结论

**内化。** 保留 `systematic-debugging` 名称和证据优先原则，将其缩减为 Auriga 自有的自适应调试契约，并通过 `auriga-workflow` 同时发布给 Claude Code 和 Codex。外部锁定版本、项目副本和兼容软链退出安装面；插件安装负责清理既有残留。

## 风险与恢复条件

- 风险：约束过轻时，模型可能在证据不足时过早结束调查。恢复信号是多次出现无法由当前契约拦住的猜测式修复。
- 风险：内化后需要 Auriga 主动吸收上游的新方法。恢复信号是上游出现经过验证、明显优于当前工具箱的诊断契约。
- 风险：旧独立副本可能遮蔽插件版本。迁移测试和真实安装验证必须覆盖项目级与用户级清理。

## 验证结果

- 技能结构校验通过。
- 仓库测试 482/482 通过。
- 会话指令加载器测试 20/20 通过。
- Git 生命周期守卫测试 103/103 通过。
- 真实安装包端到端测试 8/8 通过，覆盖远端分支获取、预设安装、筛选安装、插件安装和完整安装。

## 参考资料

- [子规范](../vendor-core-skills/systematic-debugging/spec.md)
- [验收契约](../vendor-core-skills/systematic-debugging/validation-contract.md)
- [ADR-002](../../../architecture/ADR-002-internalize-systematic-debugging.md)
