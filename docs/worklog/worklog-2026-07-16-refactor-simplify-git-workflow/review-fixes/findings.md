# 发现

- 原递归扫描器仅接收普通文件，导致 `docs/specs/` 下的规格符号链接漏检。
- 原扫描器从路径直接 `readdirSync`，会跟随根目录符号链接，并将所有读取错误当成目录不存在。
- 原 `.planning/` 扫描覆盖所有计划目录，不符合只检查活动计划的确认范围。
- 原错误消息直接拼接文件名，控制字符可污染多行输出，且没有条目或总长度上限。

以上问题均在 PR #189 首轮深入评审修复中处理。
